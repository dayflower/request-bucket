Dockerfile
.dockerignore

**/.env
**/.env.*

**/node_modules/

# .git is intentionally included so scripts/with-env.ts can embed the git
# commit hash via BUN_PUBLIC_GIT_COMMIT during the client build.
# **/.git

dist/

**/.vscode/*
